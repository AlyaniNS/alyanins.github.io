---
const { tags, active } = Astro.props;
---
<div class="flex flex-wrap gap-2">
  <a href="/?tag=All" class={`tag ${active==='All' ? 'border-brand-400 bg-brand-50 text-brand-800' : ''}`}>All</a>
  {tags.map((t) => (
    <a href={`/?tag=${encodeURIComponent(t)}`} class={`tag ${active===t ? 'border-brand-400 bg-brand-50 text-brand-800' : ''}`}>{t}</a>
  ))}
</div>
